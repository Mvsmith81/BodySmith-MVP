name: Release Forge MVP
on:
  push:
    paths:
      - 'tools/forge-release-trigger.txt'
permissions:
  contents: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Apply Forge frontend and tests
        run: python tools/apply-forge-mvp.py
      - name: Keep workflow files unchanged in the Actions push
        run: git checkout -- .github/workflows/register-smoke.yml
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Run local regression suite
        run: npm test
      - name: Run live Forge backend smoke test
        run: npm run test:forge
      - name: Commit Forge MVP production files
        run: |
          rm -f tools/apply-forge-mvp.py tools/forge-retrigger.txt tools/forge-release-trigger.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git reset .github/workflows/apply-forge-mvp.yml .github/workflows/forge-release.yml .github/workflows/register-smoke.yml || true
          git commit -m "Release BodySmith 2.5 with The Forge community MVP"
          git push
